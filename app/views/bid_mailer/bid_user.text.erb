最高入札のお知らせ

<%= @user.name %> 様

現時点で下記商品の最高額入札者です。

<%= @bid.product.name %>

入札金額 <%= number_to_currency(@bid.amount_with_tax) %>
(※この金額まで自動入札が行われます)

終了日時 <%= @bid.product.dulation_end %>

商品情報は以下のリンクから確認できます。
<%= root_url(only_path: false) %>products/<%= @bid.product_id %>



ものづくりオークション
<%= root_url(only_path: false) %>
