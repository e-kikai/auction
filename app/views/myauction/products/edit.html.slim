- set_meta_tags title: "変更・再出品"
- breadcrumb    :myauction_something, "変更・再出品"

= form_for @product, url: "/myauction/products/#{params[:id]}", html: { multipart: true, method: :patch, class: 'form-horizontal' } do |f|
  = render "form", f: f
